using Controller;using Model;namespace MAUI;public partial class RaceSimulator {        private Grid _grid;    //Labels    private Label _timer;    private Label _title;    public RaceSimulator() {        Images.Initialize();        InitialiseGrid();        DrawTrack();        Initialize();        Images.Direction = Data.currentRace.Track.startDirection;        Title = ("The great " + Data.currentRace.Track.name + " Race of " + DateTime.Now.Year);                Content = _grid;    }    //Add event handlers    private void Initialize() {        Data.currentRace.DriversChanged += DriversChanged;        Data.currentRace.RaceEnded += RaceEnded;                //The timer        _timer = new Label {            Text = Data.currentRace._timer.ToString(),            FontSize = 17,            HorizontalOptions = LayoutOptions.Center,            VerticalOptions = LayoutOptions.Center,            TextColor = Colors.Black        };        _grid.SetColumnSpan(_timer, 2);        _grid.Add(_timer, Images.XSize - 2);                //Race title        _title = new Label {            Text = Data.currentRace.Track.name,            FontSize = 30,            HorizontalOptions = LayoutOptions.Center,            VerticalOptions = LayoutOptions.Center,            TextColor = Colors.Black        };        _grid.SetColumnSpan(_title, Images.XSize);        _grid.Add(_title);        _grid.BackgroundColor = Color.FromArgb(Data.currentRace.Track.backgroundColorMaui.ToArgbHex());    }    private void InitialiseGrid() {        _grid = new Grid();        //For the track name        _grid.RowDefinitions.Add(new RowDefinition { Height = new GridLength(75) });        //Set columns        for(int i = 0; i != Images.XSize; i++) {            _grid.ColumnDefinitions.Add(new ColumnDefinition { Width = new GridLength(80) });        }                //Set rows        for(int i = 0; i != Images.YSize; i++) {            _grid.RowDefinitions.Add(new RowDefinition { Height = new GridLength(80) });        }    }    private void DrawTrack() {        Console.WriteLine("Drawing track, drivers left:" + Data.currentRace.Participants.Count);                //Set start position        Images.SetStartLocation();                //For each tile        try {            foreach (Section section in Data.currentRace.Track.Sections) {                DrawSection(section);                switch (Images.Direction) {                    case Direction.North:                        Images.Y--;                        break;                    case Direction.East:                        Images.X++;                        break;                    case Direction.South:                        Images.Y++;                        break;                    case Direction.West:                        Images.X--;                        break;                }            }                        //Reset content to let MAUI know that the grid has changed and the UI updates            _grid.BackgroundColor = Color.FromArgb(Data.currentRace.Track.backgroundColorMaui.ToArgbHex());            Content = _grid;        } catch (Exception e) {            Console.WriteLine("The track is not valid: ("+ Images.X + "," + Images.Y + ")");            Console.WriteLine(e);        }            }    private void DrawSection(Section section) {        _grid.Add(new Image {            Source = Images.GetImageSource(section)        },  Images.X,  Images.Y);                //Add the car        string carImage = Images.AddDrivers(Data.currentRace.Positions[section].Left, Data.currentRace.Positions[section].Right);        if (carImage != null) {            _grid.Add(new Image {                Source = carImage,                Rotation = Images.GetRotationOfCurrentDirection(),                Scale = 0.25                            },  Images.X,  Images.Y);        }        Images.Direction = Images.SetDirection(section);    }        private void DriversChanged(object sender, DriversChanged e) {        //Draw new track on the main thread        Application.Current?.Dispatcher.Dispatch(() => {            InitialiseGrid();            DrawTrack();        });    }        private void RaceEnded(object sender, EventArgs eventArgs) {        //End race        if (Data.currentRace.Participants.Count == 0) {            Data.currentRace = null!;            //Close window on the main thread            Application.Current?.Dispatcher.Dispatch(() => {                Data.currentCompetition.RaceInProgress = false;                Images.AwardPoints();                Navigation.PopAsync();            });        } else {            //Resubscribe to the event            Data.currentRace.DriversChanged += DriversChanged;            Data.currentRace.RaceEnded += RaceEnded;        }    }}